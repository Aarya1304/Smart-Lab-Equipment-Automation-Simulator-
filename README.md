# Smart-Lab-Equipment-Automation-Simulator-
imulate a laboratory machine that:  Accepts commands via API  Responds via socket  Logs results into SQL database  This directly matches: Python + JSON + SQL + Socket I/O + Automation
